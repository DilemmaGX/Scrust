# Looks Blocks

| Scrust Syntax | Scratch Block | Notes |
| :--- | :--- | :--- |
| `say_for(msg, secs)` | <pre class="blocks">say (msg) for (secs) seconds</pre> | |
| `say(msg)` | <pre class="blocks">say (msg)</pre> | |
| `think_for(msg, secs)` | <pre class="blocks">think (msg) for (secs) seconds</pre> | |
| `think(msg)` | <pre class="blocks">think (msg)</pre> | |
| `switch_costume_to(costume)` | <pre class="blocks">switch costume to [costume v]</pre> | |
| `next_costume()` | <pre class="blocks">next costume</pre> | |
| `switch_backdrop_to(backdrop)` | <pre class="blocks">switch backdrop to [backdrop v]</pre> | |
| `next_backdrop()` | <pre class="blocks">next backdrop</pre> | |
| `change_size_by(val)` | <pre class="blocks">change size by (val)</pre> | |
| `set_size_to(val)` | <pre class="blocks">set size to (val) %</pre> | |
| `change_effect_by("effect", val)` | <pre class="blocks">change [effect v] effect by (val)</pre> | |
| `set_effect_to("effect", val)` | <pre class="blocks">set [effect v] effect to (val)</pre> | |
| `clear_graphic_effects()` | <pre class="blocks">clear graphic effects</pre> | |
| `show()` | <pre class="blocks">show</pre> | |
| `hide()` | <pre class="blocks">hide</pre> | |
| `go_to_front_layer()` | <pre class="blocks">go to [front v] layer</pre> | |
| `go_back_layer()` | <pre class="blocks">go to [back v] layer</pre> | |
| `go_forward_layers(num)` | <pre class="blocks">go [forward v] (num) layers</pre> | |
| `go_backward_layers(num)` | <pre class="blocks">go [backward v] (num) layers</pre> | |
| `costume_number()` | <pre class="blocks">(costume [number v])</pre> | |
| `costume_name()` | <pre class="blocks">(costume [name v])</pre> | |
| `backdrop_number()` | <pre class="blocks">(backdrop [number v])</pre> | |
| `backdrop_name()` | <pre class="blocks">(backdrop [name v])</pre> | |
| `size()` | <pre class="blocks">(size)</pre> | |
